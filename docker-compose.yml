version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:80"
    environment:
      - ENVIRONMENT=production
      - PYTHONUNBUFFERED=1
    volumes:
      - ./app:/app/app
      - ./src:/app/src
    restart: unless-stopped
[supervisord]
nodaemon=true
logfile=/tmp/supervisord.log
pidfile=/tmp/supervisord.pid
user=root
loglevel=info

; ⚠️ Nginx removido - Render gerencia o tráfego na porta 8080 automaticamente
; Isso economiza ~50 MB de RAM no Free Tier

; API FastAPI (porta Render - exposta diretamente para o Render)
[program:api]
command=uvicorn app.main:app --host 0.0.0.0 --port %(ENV_PORT)s --workers 1 --loop uvloop --limit-concurrency 50
directory=/app
autostart=true
autorestart=true
startsecs=10
stopasgroup=true
environment=PYTHONUNBUFFERED=1,ENVIRONMENT=production,PORT=8080
stdout_logfile=/dev/stdout
stderr_logfile=/dev/stderr
stdout_logfile_maxbytes=0
stderr_logfile_maxbytes=0

; Dashboard Streamlit (porta 8501 - otimizado para memória)
[program:dashboard]
command=streamlit run app/dashboard.py --server.port=8501 --server.address=127.0.0.1 --logger.level=warning --client.showErrorDetails=false --server.maxUploadSize=5 --server.enableStaticServing=false --server.fileWatcherType none
directory=/app
autostart=true
autorestart=true
startsecs=30
stopasgroup=true
environment=PYTHONUNBUFFERED=1,STREAMLIT_SERVER_HEADLESS=true,STREAMLIT_BROWSER_GATHER_USAGE_STATS=false,STREAMLIT_SERVER_FILE_WATCHER_TYPE=none
stdout_logfile=/tmp/dashboard.log
stderr_logfile=/tmp/dashboard_error.log
stdout_logfile_maxbytes=5242880
stderr_logfile_maxbytes=5242880

; MLflow DESABILITADO para economizar memória (512 MB limit)
; Use MLflow localmente ou em serviço separado
; [program:mlflow]
; command=mlflow server --host 127.0.0.1 --port 5000 --backend-store-uri file:///app/mlruns --default-artifact-root file:///app/app/artifacts
; directory=/app
; autostart=false
; autorestart=false
